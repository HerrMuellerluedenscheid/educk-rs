# API server the dashboard talks to.
# Copy this file to `.env` for local development (`cp .env.example .env`).
# `just dev` points the dashboard at the locally running backend.
API_URL=http://localhost:3044
